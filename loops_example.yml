---
 - name: Loops concept
   hosts: all
   gather_facts: false
   become: yes
   tasks:
    - name: install packages
      apt:
       name: "{{item}}"
       state: present
      loop:
        - git
        - tomcat
        - httpd